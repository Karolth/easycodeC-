@{
    ViewData["Title"] = "Crear Nueva Ficha";
    Layout = null; // Puedes usar un layout si lo tienes definido, por ejemplo, "_Layout.cshtml"
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="~/public/css/Modulo.css">
</head>
<body>
    <div class="container">
        <h1>Crear Nueva Ficha</h1>

        @*
            NOTA IMPORTANTE:
            La acción del formulario debe apuntar a un controlador y una acción en ASP.NET Core.
            Asegúrate de tener un controlador (ej. FichasController) con un método de acción (ej. GuardarFicha)
            que maneje la lógica de guardar la ficha y los aprendices.
            El 'enctype="multipart/form-data"' es correcto para el envío de archivos.
        *@
        <form id="formFichaCompleta" asp-action="GuardarFicha" asp-controller="Fichas" method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="programaFormacion">Programa de Formación:</label>
                @*
                    El atributo 'action' en el elemento <select> no es un atributo HTML estándar.
                    Normalmente, la carga de opciones para un <select> se haría:
                    1. Desde un modelo pasado a esta vista.
                    2. Vía JavaScript (Ajax) haciendo una llamada a un endpoint de tu API/controlador.
                *@
                <select id="programaFormacion" name="programaFormacion">
                    <option value="">Seleccione un programa</option>
                    @* Aquí irían las opciones de programas cargadas dinámicamente, por ejemplo: *@
                    @* @foreach (var programa in Model.Programas)
                    {
                        <option value="@programa.Id">@programa.Nombre</option>
                    } *@
                </select>
            </div>
        
            <div class="form-group">
                <label for="jornada">Jornada:</label>
                <select id="jornada" name="jornada" required>
                    <option value="">Seleccionar Jornada</option>
                    <option value="mañana">Mañana</option>
                    <option value="tarde">Tarde</option>
                    <option value="noche">Noche</option>
                </select>
            </div>
        
            <div class="form-group">
                <label for="tipoPrograma">Tipo de Programa:</label>
                <select id="tipoPrograma" name="tipoPrograma" required>
                    <option value="">Seleccionar Tipo de Programa</option>
                    <option value="auxiliar">Auxiliar</option>
                    <option value="tecnico">Técnico</option>
                    <option value="operario">Operario</option>
                    <option value="tecnologo">Tecnólogo</option>
                </select>
            </div>
        
            <div class="form-group">
                <label for="fechaInicio">Fecha de Inicio:</label>
                <input type="date" id="fechaInicio" name="fechaInicio" required>
            </div>
        
            <div class="form-group">
                <label for="fechaFin">Fecha de Fin:</label>
                <input type="date" id="fechaFin" name="fechaFin" required>
            </div>
        
            <div class="form-group">
                <label for="numeroFicha">Número de la Ficha:</label>
                <input type="text" id="numeroFicha" name="numeroFicha" required autocomplete="off">
            </div>
        
            <div class="form-group">
                <label for="archivoExcel">Archivo de aprendices (.csv):</label>
                <input type="file" id="archivoExcel" name="archivoExcel" accept=".csv,.xlsx" required>
            </div>
        
            <div class="button-group">
                <button type="submit" class="btn-submit" id="btnSubmit">Crear Ficha</button>
                <button type="button" id="btnCancelar">Cancelar</button>
            </div>
        
            <div id="mensajeExito" class="success-message" style="display: none;">
                ¡Ficha creada exitosamente con aprendices!
            </div>
        </form>

    <script src="~/public/js/Modulo.js"></script>
</body>
</html>